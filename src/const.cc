#include <mympz/mympz.h>
#include "__internal.h"

namespace mympz {

const bignum_t const_null = create("null");
const bignum_t const_zero = create(0);
const bignum_t const_one = create(1);

} // namespace mympz